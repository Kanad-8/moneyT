import { NormalizeInput } from './normalize-input';

describe('NormalizeInput', () => {
  it('should create an instance', () => {
    const directive = new NormalizeInput();
    expect(directive).toBeTruthy();
  });
});
