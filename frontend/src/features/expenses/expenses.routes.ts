import { Routes } from '@angular/router';
import { ExpensePage } from './page/expense-page/expense-page';

export const EXPENSE_ROUTES: Routes = [
    {
        path:'',
        component:ExpensePage
    }
];